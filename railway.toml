[build]
builder = "dockerfile"
buildArgs = ["VITE_API_URL=https://backend-production-7571.up.railway.app"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
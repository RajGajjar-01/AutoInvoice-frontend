// Backwards-compatibility shim — layout refactored to Notion-style.
// The left panel is now ColumnEditorPanel on the right side.
export { ColumnEditorPanel as LeftPanel } from "./ColumnEditorPanel"
